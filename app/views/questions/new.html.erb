<h1>New Question</h1>

<%= form_for @question do |f| %>
  <div class="form-group">
    <%= f.label :post, "Question" %>
    <%= f.text_area :post %>
    <%= f.hidden_field :user_id, value: current_user.id %>
  </div>
  <%= f.submit "Post", :class => "btn btn-primary" %>
<% end %>
